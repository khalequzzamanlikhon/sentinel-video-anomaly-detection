# Core (Tier 1) -- everything needed to run detection, tracking, rule-based anomaly
# detection, alerting, the dashboard, and the test suite.
opencv-python>=4.9
numpy>=1.24
ultralytics>=8.2
lap>=0.5           # linear assignment for BoT-SORT/ByteTrack matching (ultralytics track())
PyYAML>=6.0
streamlit>=1.32
pandas>=2.0
prometheus-client>=0.20

# Dev / test
pytest>=8.0
